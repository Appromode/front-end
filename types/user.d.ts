type User = {
  firstName: string;
  lastName: string;
  normalizedEmail: string;
  normalizedUserName: string;
  profilePicture: number;
  twoFactorEnabled: boolean;
  userId: string;
};

export default User;
